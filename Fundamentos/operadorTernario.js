// (expressão) ? (verdadeiro) : (false)

const number = 4;

console.log(number % 2 == 0 ? "Par" : "Impar");

const learnedJs = false;
const withRincko = false;

console.log(learnedJs && withRincko ? "Aprendeu Js com o Rincko" : learnedJs ? "Aprendeu JS, mas não foi com o Rincko" : "Não sabe JS");